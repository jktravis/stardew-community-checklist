import styled from "@emotion/styled";
import Box from "./Box";

const ExpandButtonStyle = styled(Box)`
  display: flex;
  justify-content: flex-end;
  background-color: transparent;
  border: none;
  box-shadow: none;
`;

export default ExpandButtonStyle;
